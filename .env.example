# Kimi API (required for token-proxy to forward LLM calls)
KIMI_API_KEY=sk-your-kimi-api-key
KIMI_BASE_URL=https://api.moonshot.cn/v1

# Only needed if operator will provision real K8s pods
# KUBECONFIG=~/.kube/config
