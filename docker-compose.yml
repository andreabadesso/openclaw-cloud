# Build-only — runtime is in k3d. Use scripts/dev-setup.sh to deploy.
services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    image: ghcr.io/andreabadesso/openclaw-cloud/api:latest

  token-proxy:
    build:
      context: .
      dockerfile: apps/token-proxy/Dockerfile
    image: ghcr.io/andreabadesso/openclaw-cloud/token-proxy:latest

  operator:
    build:
      context: .
      dockerfile: apps/operator/Dockerfile
    image: ghcr.io/andreabadesso/openclaw-cloud/operator:latest

  browser-proxy:
    build:
      context: apps/browser-proxy
      dockerfile: Dockerfile
    image: ghcr.io/andreabadesso/openclaw-cloud/browser-proxy:latest

  web:
    build:
      context: apps/web
      dockerfile: Dockerfile
    image: ghcr.io/andreabadesso/openclaw-cloud/web:latest
