# Build-only — runtime is in k3d. Use scripts/dev-setup.sh to deploy.
services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    image: openclaw-cloud-api:latest

  token-proxy:
    build:
      context: .
      dockerfile: apps/token-proxy/Dockerfile
    image: openclaw-cloud-token-proxy:latest

  operator:
    build:
      context: .
      dockerfile: apps/operator/Dockerfile
    image: openclaw-cloud-operator:latest

  web:
    build:
      context: apps/web
      dockerfile: Dockerfile
    image: openclaw-cloud-web:latest
